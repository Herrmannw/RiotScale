---
const baseUrl = import.meta.env.BASE_URL;
const pageHref = (path = "") => {
  const base = baseUrl.endsWith("/") ? baseUrl : `${baseUrl}/`;
  return `${base}${path}`;
};

const navItems = [
  { label: "Home", href: pageHref() },
  { label: "About Us", href: pageHref("about-us/") },
  { label: "Services", href: pageHref("services/") },
  { label: "Contact", href: pageHref("contact/") },
];

const contactHref = pageHref("contact/");
---

<header class="navbar" aria-label="Primary navigation">
  <div class="navbar-inner container">
    <a class="brand" href={pageHref()} aria-label="RiotScale home">RIOTSCALE</a>

    <nav class="nav-links" aria-label="Main menu">
      {
        navItems.map((item) => (
          <a href={item.href} aria-label={`${item.label} section`}>
            {item.label}
          </a>
        ))
      }
    </nav>

    <a class="button button-primary nav-cta" href={contactHref}>Let's Talk</a>

    <details class="mobile-menu">
      <summary aria-label="Open navigation menu">
        <span></span>
        <span></span>
        <span></span>
      </summary>

      <nav class="mobile-menu-panel" aria-label="Mobile menu">
        {
          navItems.map((item) => (
            <a href={item.href} aria-label={`${item.label} section`}>
              {item.label}
            </a>
          ))
        }
        <a class="button button-primary mobile-menu-cta" href={contactHref}>Let's Talk</a>
      </nav>
    </details>
  </div>
</header>
